import React from 'react';
//import PropTypes from 'prop-types'

function Header() {
    return (
        <header>
            <div className="container">
                <h2>Feedback UI</h2>
            </div>
        </header>
    );
};

export default Header;